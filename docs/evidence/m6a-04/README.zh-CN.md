# M6A-04 接口验证 — 2026-09-21

[English](./README.md) | 简体中文

[软件 CI 回执](./software-ci.json)绑定PR 源码 `7b55810` 及受测合并提交 `f0e6c0a`（完整 SHA 以回执为准）、精确候选归档、安装候选的 13 项浏览器测试、独立注册表的九项测试，以及四组 1K 资源／八张 Native／浏览器通道对照。该源码六项 PR 检查全部通过。[浏览器运行](https://github.com/OpenMixture/OpenMixture/actions/runs/35593454452)使用固定软件矩阵；全部准备计划身份一致，所有分量差均满足不变的 ≤1 门槛。90,249,091 字节产物已下载，并在记录的检索日期核对所选报告。远程产物于 2026-10-21 到期，本地完整报告和 PNG 是临时副本，不是持久归档。本摘要仅证明该范围的机器集成结果，不代表发布或人工图像验收。

[Windows 硬件失败](./windows-hardware-failure.json)仍是失败结果。干净源码候选通过全部 13 项接口测试，但 Native GT 1030／Vulkan 对 Chrome WebGPU 的法线最大差在权重 0.5 为 4、权重 1 为 8；全部高度最大差 ≤1，权重 0 完全一致。记录包含源码／测试哈希、归档身份、适配器证据、一致的准备计划哈希、最差像素样本及原始产物哈希。权重 1 在两个运行时分别等于直接程序噪声端点，图像上传不贡献该端点。本次未通过修改着色器、限制或 golden 隐藏精度失败；比较器在该路线仍以非零退出。本摘要之外不承诺原始本地产物的长期保留。

后续比较器记录修正仅在 Native 像素、浏览器构建身份及全部计划哈希均通过后写入成功；单独的 Native 报告不能让外层比较被接受。最终 PR 检查独立验证后续修订，不重标这些有日期的回执。[复现与资格范围](../../m6a-04-browser-resources.zh-CN.md)保持权威。M6A-05 必须先解决或明确限定硬件精度问题，才能扩展发布声明。
