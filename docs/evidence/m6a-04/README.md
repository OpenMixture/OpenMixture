# M6A-04 interface verification — 2026-09-21

English | [简体中文](./README.zh-CN.md)

The [software CI receipt](./software-ci.json) binds PR head `7b55810` and its tested merge `f0e6c0a` (full SHAs in the receipt), its exact candidate archive, 13 installed-candidate browser tests, nine separate registry tests and four 1K resource cases/eight Native/browser channel comparisons. All six PR checks passed on that source. [The browser run](https://github.com/OpenMixture/OpenMixture/actions/runs/35593454452) uses the pinned software matrix; every prepared plan identity matches and every component delta satisfies the unchanged ≤1 gate. The downloaded 90,249,091-byte artifact's selected reports were checked on the recorded retrieval date. Remote expiry is 2026-10-21; local full reports and PNGs are temporary, not a durable archive. This retained summary certifies that bounded machine integration result, not publication or human image acceptance.

The [Windows hardware failure](./windows-hardware-failure.json) remains a failed result. The clean source-built candidate passes all 13 interface tests, but Native GT 1030/Vulkan versus Chrome WebGPU yields normal maxima 4 at weight 0.5 and 8 at weight 1; all height maxima are ≤1 and weight 0 is exact. The record includes source/test hashes, archive identity, adapter evidence, equal prepared hashes, worst-pixel samples and original artifact hashes. Weight 1 equals the direct procedural endpoint independently in each runtime; image upload does not contribute to that endpoint. No shader, limit or golden was changed to hide this precision failure. The comparator still exits nonzero on this route. Full original local output is not promised beyond this summary.

The subsequent comparator bookkeeping change writes success only after Native pixels, browser build identity and all plan hashes pass; a Native report alone cannot mark the outer comparison accepted. Its final PR checks certify that later revision separately from these dated receipts. [Reproduction and qualification scope](../../m6a-04-browser-resources.md) remain authoritative. M6A-05 must resolve or explicitly scope the hardware precision issue before broader release claims.
