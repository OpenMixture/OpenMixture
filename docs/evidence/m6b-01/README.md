# M6B-01 entry evidence

English | [简体中文](./README.zh-CN.md)

[Measurement](./measurement.json) records the CPU-only [portable asset probe](../../../scripts/probe-portable-assets.mjs) on 2026-09-22, Windows, against a CLI built with `cargo build --locked -p mixture-cli` from main `c2a08e14be9991d75df24e0f045d842d8db4b570`. The working tree was dirty because the new probe was not committed; runtime crates, Cargo.toml and Cargo.lock were unchanged. Exact probe, binary, source and synthetic resource hashes are retained. The `.mix` fixture is committed; raw pixels can be regenerated by the probe.

Both source-only validation and resource-free compilation passed. Height/normal compilation failed with `MIX_RESOURCE_MISSING` before and after putting raw bytes beside the document. The report retains original structured errors and exit codes. This is an expected baseline limitation, not a failing new runtime check. Representation-size measurements establish base64 overhead only; no performance, GPU or binary-format acceptance follows.

Run `node scripts/probe-portable-assets.mjs <fresh-output> [baseline-cli]` after building the baseline. No package commands have been introduced. The [phase plan](../../m6b-portable-assets.md) separates requirements from format selection and implementation. Full CLI build/check logs and generated raw files remain in ignored `tmp/`; no permanent external archive is claimed. The retained JSON plus source/probe are sufficient to inspect this CPU observation and reproduce a fresh measurement, not to authenticate an unavailable original binary.
