# M6B-04 local adapter verification

English | [简体中文](./README.zh-CN.md)

[PR #44](https://github.com/OpenMixture/OpenMixture/pull/44) implements the [CLI/browser asset contract](../../m6b-04-adapters.md). [receipt.json](./receipt.json) binds local verification to clean implementation commit `3fef6093f186e56b2d1311722ccb03711df69dd8`; this later documentation commit is not the tested runtime revision. No package is published or PR merged.

`cargo xtask check` passes, including independent source and isolated archive consumers. Runtime contracts pass 9 tests, qualification guards 6, actual public WASM all 22 codec cases. Windows Native DX12 (NVIDIA GeForce GT 1030) passes 15 CLI invocations. The exact clean npm archive passes all 15 independent Windows Chrome cases without skips or retries. Browser adapter identity is reported as `BrowserWebGpu`, with no exposed GPU name; do not infer its physical adapter from the Native result. Package/loose and Native/browser plans share the recorded hash; all 195 height pixels equal `[128,128,128,255]` after the 65×3 moved-asset workflow. No visual semantics or golden baselines changed.

Public WASM inspection measures one JS snapshot and one Rust copy: the 4,197,888-byte normal archive charges 8,396,646 bytes, and the 67,118,592-byte maximum-resource archive charges 134,240,169 bytes including source/manifest scratch. One byte below each measured budget rejects. These are actual buffer-capacity ledger checks, not process RSS or full-size GPU acceptance. The normal fixture has one 1024×1024 image; the maximum has eight 2048×1024 images (64 MiB raw total). All repeat `[128,37,91,255]`. To reproduce, derive the graph from the tracked browser asset source, add disconnected `image-input` nodes `image1`…`image7` with resource IDs `Input1`…`Input7` for the maximum case, author via `mixture asset pack`, then pass both archives to the public WASM verifier after its package-directory argument. Exact source/resource identities are retained in the receipt.

The bundled Chromium attempt failed at process launch (`spawn UNKNOWN`), before 14 page tests; one static-build test passed. The separate installed Chrome attempt passed. This is explicitly not a passing bundled-Chromium record. Full failure and success logs remain under ignored `tmp/m6b04-browser-consumer` and `tmp/m6b04-browser-chrome`; their original continued availability is not promised. The source tests and tracked fixtures reproduce new results.

Reproduction commands are in the adapter guide. For this Windows browser attempt set `MIXTURE_BROWSER_CHANNEL=chrome`; for the Native ignored `cli_contract_gpu` test set `MIXTURE_GPU_BACKEND=dx12`, `MIXTURE_GPU_SOFTWARE=0`, `MIXTURE_CONSUMER_CLI` to the built executable and `MIXTURE_CONSUMER_EVIDENCE_DIR` to a fresh absolute directory. The native consumer has its independent Cargo manifest. Full logs/archives/PNG outputs and large generated packages remain ignored local artifacts; this summary retains the decisive measurements. Remote CI was pending when recorded. Full M6B-05 material/package qualification, six required checks and publication remain separate.
