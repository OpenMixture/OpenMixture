# MAT-02 morphology node evidence

English | [简体中文](./README.zh-CN.md)

This record binds **clean `ad70106fae586777597cf885268e1d19a66fe5d6`**, unpublished Rust 0.7.0 / browser 0.7.0-alpha.0. It verifies the morphology node on the recorded Windows GT 1030, not the painted-metal material, all hardware, publication or completed MAT-02. [PR #56](https://github.com/OpenMixture/OpenMixture/pull/56) remains subject to all six checks.

[Browser qualification](./browser-qualification.json) passed all 18 tests using the exact [archive identity](./archive-receipt.json). The [192 browser cases](./browser.json) cover four sizes, four fixed support sets, both axes, both operations and radii 0/1/16. [Vulkan](./comparison-vulkan.json) and [DX12](./comparison-dx12.json) each match every plan hash and RGBA8 component exactly (maximum difference 0). [Native Vulkan pixels](./native-vulkan.json) and [Native DX12 pixels](./native-dx12.json) retain complete outputs and adapter details. Repeats are exact. The [support sheet](./support-review.png) enlarges selected browser pixels without filtering; it is agent inspection, not a human PBR decision. The binding [receipt](./receipt.json) preserves hashes and qualification limits.

The full local `cargo xtask check` passed at this source, including packaged consumption. Earlier development checks found an ABI fixture outside the package and a Clippy fixed-chunk rule; both were repaired before this source. The software [CI run](https://github.com/OpenMixture/OpenMixture/actions/runs/35826916523) **failed** an old cache-count assertion (`seen.len() <= 12`); its new node probes passed. The retained [stdout](./ci-failure.stdout.log) and [stderr](./ci-failure.stderr.log) preserve that distinction. The following test repair uses the explicit reviewed kernel identity set rather than a stale count. These Windows pixels do not claim that failed CI was successful, and later test/documentation commits are not the source measured here.

Reproduce from this source with `node scripts/browser-runtime/build.mjs`, then `MIXTURE_BROWSER_CHANNEL=chrome node scripts/browser-runtime/consumer.mjs candidate target/browser-runtime <fresh-browser-output>`. Run `node scripts/browser-runtime/check-morphology.mjs <fresh-browser-output> <fresh-comparison-output>` separately with explicit `MIXTURE_GPU_BACKEND=vulkan` or `dx12`, `MIXTURE_GPU_SOFTWARE=0`. Follow [node fixtures](../../../fixtures/nodes/scalar-morphology/README.md) for raw half probes. Powershell users set these environment variables with `$env:` assignments. The original archive bytes, compiler products and ordinary logs remain ignored output or expiring CI artifacts; the selected receipts/pixels/failure logs are retained here. No durable external archive is claimed.
