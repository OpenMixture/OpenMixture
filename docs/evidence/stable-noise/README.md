# NUM-01: versioned stable value noise

English | [简体中文](./README.zh-CN.md)

On 2026-09-22 the maintainer authorized explicit noise-semantic migration and material requalification. [PR #40](https://github.com/OpenMixture/OpenMixture/pull/40), stacked on [investigation PR #39](https://github.com/OpenMixture/OpenMixture/pull/39), implements the [Q0.24 contract](../../stable-noise.md). This is an unpublished 0.4 candidate. The measured Windows resource/Scalar regression is repaired for explicitly migrated v2 value-noise inputs; this does not certify arbitrary hardware graphs, cellular noise, or the separately documented warp limitation.

## Source-bound results

- **Windows package:** clean `2cadd6deb5a9b38263bbcbe6a1dbbc7bbeb0a26c`, archive SHA-256 `8d1801d7f4b8d6ce480f1f99ebfc1c88bea6a0c936e08548f3596964638d5caa`, build `sha256:bc7a5acda8b047f6098c94a6c2ec4e8f8a1463ad7825104a9b053d1c73bf94f8`. [SDK receipt](./windows-sdk.json): all 13 public browser tests passed, no skips or flaky cases. Chrome 153.0.8010.48 selected NVIDIA/Pascal, non-fallback. Native selected GeForce GT 1030, NVIDIA 582.66 / DX12 driver 32.0.15.8266.
- **Public 1K pixels:** all four weights (0, 0.25, 0.5, 1), both height/normal channels, both Vulkan and DX12, match Chrome exactly: max component delta **0**, versus the historical resource-normal max **8**. The original ≤1 gate is unchanged. Retained reports: [Vulkan resources](./resources-vulkan.json), [DX12 resources](./resources-dx12.json), [Vulkan Scalar](./scalar-vulkan.json), [DX12 Scalar](./scalar-dx12.json), [browser context](./browser-resources.json).
- **Raw diagnostic probes:** [six parameter sets](./raw-probes.json), including scale 7/13/128, maximum seed, six octaves and persistence 0/0.1/0.5/1, produce zero raw f16 height and normal differences on both Native backends versus Chrome. Vulkan/Chrome captures preceded the clean source commit; exact shader/parameter/output hashes bind them separately. DX12 reran the same production shader after `991ae1d`. These probes supplement the clean package receipt; they are not package qualification themselves.
- **CI:** all [six required checks](./ci-checks.json) passed for head `991ae1d2302322b5302f460f77c43532e26cb0e3`. The material job tested synthetic merge `dae2cf58ccde3a1cf23063bf6084fc99e9ecaa9a`; its independently built archive SHA-256 is `fc47727724b0e2ddd4ffff7f47b52794aa68758ddff7097a468720797dd40f8f`. The [separate migration qualification](./ci-noise-v2-qualification.json) binds the unchanged public/deployment gates and all 11 migrated cases / 44 channels. [Comparison](./ci-material-comparison.json): ceramic and wood max delta 0; cellular leather max 1. Structure, causality, relationships and numerical gates all pass. The original matrix also passed. CI is pinned software GPU evidence, not Windows material qualification.
- **Local checks:** `cargo xtask check`, `shader-check`, `test-core`, `test-plan`, Vulkan and DX12 `test-node fractal-noise`, and Vulkan `gpu-smoke` passed. The latter includes independent Rust/CLI and isolated package GPU consumption. The new exact v2 node baseline also passed pinned SwiftShader CI. [Plan compatibility](./plan-compatibility.json) confirms all 11 legacy material plans retain their intended hashes; original goldens were not replaced.

## Visual migration review

The Windows Vulkan before/after renders used the clean package-source revision above, 1024×1024, all four channels and 11 existing cases. [Full-resolution measurements](./migration-comparison.json) bind source/plan/PNG identities and the retained contact sheets. Ceramic and cellular leather are byte-identical; wood changes only sparse components by 1/255. Agent inspection of the default/coarse wood, detail-max leather and fine ceramic sheets found preserved material structure, orientation and channel relationships without visible new artifacts. CI default sheets were also inspected. This records **agent review**, not a claim that a human approved the new images. No baseline was reset and no package was published.

Each `<material>-<case>.png` has v1 / v2 / absolute difference ×16 columns, downsampled from 1024 to 320 pixels. Small isolated differences can disappear in these previews; use the full-resolution metrics for their extent. Each `ci-<material>-<case>.png` is the original Native/browser/×4 comparison from the CI verifier. All 22 sheets are retained in Git. The [review binding](./visual-review.json) records their digests and review scope. Representative sheets: [wood migration](./wood-default.png), [wood CI](./ci-wood-default.png), [leather CI](./ci-leather-default.png).

## Reproduction and retention

Use the exact tested source for receipt reproduction; later evidence-only commits are not the original tested build. Follow the [contract's migration commands](../../stable-noise.md). Build with `node scripts/browser-runtime/build.mjs`, then run `consumer.mjs candidate target/browser-runtime <fresh-sdk-directory>` under `scripts/browser-runtime/` with `MIXTURE_BROWSER_CHANNEL=chrome`. Run `check-resources.mjs` and `check-scalar.mjs` against that receipt with `MIXTURE_GPU_BACKEND=vulkan`, then `dx12`, each using fresh output directories. These checks require the receipt's consumer revision to equal HEAD.

For before/after images, prepare `tmp/stable-noise/native-materials-v1` normally and `tmp/stable-noise/native-materials-v2` with `--noise-v2`, explicit Vulkan, and the same full runtime revision. `python docs/evidence/stable-noise/make-review.py` uses Pillow/NumPy only to compare already-rendered PNGs and produce the retained 11 sheets; it does not render material pixels. The browser-material CI workflow reproduces both material matrices, exact candidate installation and the separate migration receipt.

[Artifact metadata](./ci-artifacts.json) records material artifact `10675392337`, 154,202,040 bytes, expiry **2026-10-22T03:28:55Z**, retrieved and inspected on 2026-09-22. Full logs, raw f16 captures, archives, manifests and full-resolution repeated material outputs remain in ignored local directories or expiring CI artifacts. No permanent external archive is claimed. Git retains selected receipts, measurements and the actual reviewed contact sheets; it does not retain enough bytes to re-audit every original full-resolution pixel after artifact expiry. The [historical v1 failure](../windows-numerics/README.md) remains unchanged.
