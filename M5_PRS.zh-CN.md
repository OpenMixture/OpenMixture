# M5 实施计划——浏览器运行时与独立 Player

[English](./M5_PRS.md) | 简体中文

**当前状态（2026-09-20）：** 原生 M4／M4.1、有界 M5、记录范围内的 Studio MVP 及普通 Windows Chrome／Edge／Firefox 验收已完成。npm Alpha `@openmixture/runtime@0.1.0-alpha.0` 已发布，准确注册表版本消费已通过记录的 Studio 门槛。Rust crate 仍未发布；两项浏览器检查是 main 必需检查。[Alpha 收口](./docs/browser-alpha.zh-CN.md)负责当前工作；不启动 M6。

以下带日期的检查点保留当时状态，不作为当前未完成项清单。

**M5 验收，2026-09-15：** [浏览器验收记录](./docs/evidence/m5-05/README.zh-CN.md)关闭记录的 macOS／Linux Chromium 矩阵内 M5-05 门槛：冻结后两端各通过 11 个 1K 用例／44 通道比较、语义与质量检查、12 次额外生命周期渲染，以及独立产品隔离安装、28 项浏览器契约和正常生产静态部署。完整像素与来源证据已保留。Alpha 就绪限于实测范围，npm 仍未发布；Studio／M6 需另行决定。下方较早的日期记录保留其当时状态。

**Player 导出更新，2026-09-15：** M5-04 的打开 → 调参 → 通道预览 → PNG 下载流程已在独立产品完成本地验收。干净隔离消费者通过 28 项 Chromium 检查及九项 Node 测试；三种材质的 12 份 128×128 通道 PNG 经独立解码，像素与公开运行时结果逐字节一致，sRGB／线性元数据正确。另验证八通道 65×3 下载、过期导出抑制和编码失败。[产品证据](https://github.com/OpenMixture/Studio/blob/77f00deb5410b73140220fabe31f4f8599b3279d/docs/evidence/m5-04-export/README.zh-CN.md)绑定确切源码及未变更运行时归档。M5-05 的 1K 跨端质量、压力、正式浏览器 CI 和部署／兼容性验收仍开放；未执行发布。

**Player 更新，2026-09-14：** 独立产品已实现并[完成本地验证](https://github.com/OpenMixture/Studio/blob/7370e482e2dcacb9911f5663f8ec4f9e8da6a4cc/docs/evidence/m5-04-parameters/README.zh-CN.md) M5-04 参数／预览切片：Rust 元数据控件、通道选择、一个活动渲染加一个可替换待处理请求、过期预览诊断及生命周期清理。干净隔离消费者通过 23 项 Chromium 检查和六项 Node 测试，包括三材质 128×128 预览。PNG 导出和完整 M5-04／M5-05 验收仍开放；运行时归档未变，仍未发布。

**本地验收更新，2026-09-14：** M5-02／M5-03 初始浏览器执行与隔离软件包消费现已通过[记录的门槛](./docs/evidence/m5-02-03/README.zh-CN.md)。未变更归档通过 13 项 Chromium 检查，包括可控真实设备丢失、映射清理及重复独立模块／设备。M5-04／M5-05 材质回归、完整 Player 流程与正式浏览器 CI 矩阵仍开放；软件包未发布。

**实施更新，2026-09-12：** 产品仓库 [OpenMixture/Studio](https://github.com/OpenMixture/Studio)已创建。首个 M5-02／M5-03 切片已实现 `mixture-wasm`、本地 `@openmixture/runtime@0.1.0-alpha.0` tarball，以及消费它的最小 Player。[浏览器启动指南](./docs/browser-runtime.zh-CN.md)记录实际构建／消费命令及当前验证。完整 M5 验收仍开放，包括材质回归、更广浏览器失败／压力覆盖和完整 Player 导出流程。软件包仍未发布。

`M5-01` 至 `M5-05` 是工作项标识，不是 GitHub PR 编号。实际实施提交、PR URL 及检查存在后再记录。工作遵循[仓库治理](./docs/governance.zh-CN.md)、[证据保留](./docs/evidence-policy.zh-CN.md)及现有[架构](./ARCHITECTURE.zh-CN.md)。[浏览器 SDK 契约](./docs/browser-sdk.zh-CN.md)记录已实现的公开方法及剩余验收要求。通过当前检查点及运行证据区分实现与已接受的门槛。

## 目标与仓库边界

交付一个由引擎仓库构建、可安装的浏览器运行时，以及一个只使用其公开软件包的独立 Player。`.mix`、节点契约、编译和 WGSL 语义继续由原生消费者所使用的同一组 Rust 库负责。

| 所有者 | M5 职责 |
|---|---|
| 现有 `OpenMixture/OpenMixture` | `mixture-core`、唯一的 `mixture-wgpu` 执行器、原生 CLI、轻量 `mixture-wasm`、浏览器 JS 接口／类型／WASM，以及运行时软件包验证。 |
| `OpenMixture/Studio` 产品仓库 | 先交付 Player：文件、公开参数控件、2D 通道预览、请求新鲜度和导出。后续 Studio 可共用内部 runtime-client、预览、控件和文件模块。 |
| 本地 `@openmixture/runtime` npm 包 | 完整浏览器运行时的公开分发边界；在引擎仓库构建和管理版本，不另建 SDK 仓库。 |

产品仓库现已存在，并在 M5-03 中消费首个本地运行时归档。包名标识当前本地分发；npm 所有权与 Registry 发行仍是发布前的独立检查。不在产品 Player 之外再建一个完整演示应用；引擎侧的小型自动化浏览器夹具仍然适用。

```text
Product Player (later Studio)
    -> @openmixture/runtime (JS + TypeScript declarations + WASM)
        -> mixture-wasm
            -> mixture-core + mixture-wgpu
```

首个 npm 包只承诺浏览器 WebGPU。它不要求先将 Rust crate 发布到 crates.io，产品开发者安装时也不得需要 Rust 或编译引擎。Cargo 发布政策与 npm 分发分开处理。

## 实施顺序

| 工作项 | 所有者 | 前置条件 | 可评审结果 |
|---|---|---|---|
| M5-01 | 引擎文档 | M4／M4.1 已验收 | 成对计划／SDK 契约、同步路线图、明确范围与门槛。 |
| M5-02 | 引擎 | M5-01 | 通过轻量绑定完成真实浏览器初始化、验证、棋盘格执行、自有像素回读及清理。 |
| M5-03 | 引擎与独立产品各自的 PR | M5-02 | 自包含 npm tarball，以及消费该确切归档、经过生产构建的 Player 页面。 |
| M5-04 | 产品及必要的引擎契约暴露 | M5-03 | 带公开参数、通道预览、诊断、导出及有界最新请求处理的 Player MVP。 |
| M5-05 | 两个仓库 | M5-04 | 浏览器／原生回归、干净安装／部署证据、兼容性记录及 Alpha 就绪评估。 |

每项可以包含多个 PR；每个 PR 保持一个架构边界。引擎与产品变更不能跨仓原子合并：先准备引擎产物，在产品变更中标明其版本与摘要，任一方变化后重跑验收。契约评审后，产品基础工程可独立推进；运行时验收始终使用真实打包引擎。

## M5-01——交付与 SDK 契约

**范围**

- 新增成对实施计划与浏览器 SDK 契约；同步当前路线图、架构引用及导航。
- 定义显式 WASM 加载与 GPU 获取、无需 GPU 的目录／验证访问、原始源输入、公开覆盖、请求通道、自有输出、结构化失败及生命周期规则。
- 保留现有 `.mix v1`、十一个节点契约、九个 kernel、原生 API 行为及已接受的基准输入／像素。公开参数绑定已经存在；通过 SDK 暴露其含义，不增加产品私有映射。
- 首发消费方式选为浏览器 ESM 加 Vite，确切工具版本和浏览器环境由实施记录在[浏览器启动指南](./docs/browser-runtime.zh-CN.md)与产品锁文件中。单独的规划不建立浏览器／平台支持声明。

**验收**

两种语言包含相同范围、标识符与要求。所有本地链接可解析。剩余要求可与已实现 API／工具区分。文档变更通过 `cargo xtask check`，其真实 PR 在集成之前通过现有必需检查。本项不记录浏览器通过或发布验收。

**不在范围内：** 运行时或依赖变更、创建产品仓库、npm 发布、节点／编辑器实现，以及修改历史证据或 `mixture-greenfield-docs/`。

## M5-02——浏览器执行与轻量绑定

**范围**

- 为实际 WASM 编译／绑定边界引入 `mixture-wasm`。浏览器传输留在绑定中，平台执行行为留在类型化 `mixture-wgpu` 模块中。
- 审查目标平台 wgpu features、适配器／设备限制、异步错误作用域、回调完成、设备丢失处理和计时。现有原生等待／polling 路径不代表浏览器已就绪；浏览器回读必须让出执行权等待映射完成，不阻塞事件循环。
- 显式加载 WASM，暴露 CPU 侧验证，显式获取 GPU 实例，通过共享编译器／执行器渲染棋盘格 `.mix`，返回独立 RGBA8 并释放资源。
- 实现契约规定的单实例并发与销毁行为；对不支持的环境、初始化、无效输入和执行提供有意义的失败。保留原生行为及现有检查。
- 选择并记录首个可复现的浏览器／OS／适配器／工具链组合。它代表测试过的环境，不承诺所有浏览器、移动设备或硬件。

**验收**

真实浏览器加载并验证棋盘格，渲染正确的请求像素，在再次渲染和销毁后消费结果，并且不使用其他渲染器来掩盖失败。验证目录／验证操作不获取 GPU。在可观察的浏览器边界检查映射／清理和设备丢失行为；区分注入的错误分类测试与真实设备测试。仅 WASM 构建成功或模拟 JS 测试不能通过本项。

运行受影响的原生检查、适用的 shader 验证、`cargo xtask check`，并为生命周期／回读变更执行显式原生 GPU 回归。浏览器测试／构建实现时添加可复现指令；不得把原生 `gpu-smoke` 悄然改成浏览器测试。

**不在范围内：** Player 功能界面、新节点、语义性 shader 变更、隐式回退、WebGL2／CPU 执行、GPU 纹理互操作及宽泛性能优化。

## M5-03——打包运行时与独立加载页

**范围**

- 从同一个可标识构建生成 `@openmixture/runtime`，包含公开 ESM 入口、TypeScript 声明、生成绑定、`.wasm`、README 和许可证。测试实际公开入口与资源位置覆盖。
- 构建真实 npm tarball，由独立产品仓库消费。选择确切的 Vite／Node／包管理器版本并保留产品锁文件。安装／构建不得依赖 Rust、生产方源码路径、开发软链接或编译引擎的 postinstall。
- 实现最小 Player 页面：加载 `.mix`、显式初始化运行时、显示返回的通道。静态服务下验证 Vite 生产构建，包括非根 base path 下的 WASM 资源路径。
- 记录生产方版本／构建／锁／归档摘要，以及消费方版本／锁。验收运行应能获得 tarball；不得将开发者的绝对本地路径提交为可复现安装步骤。

已实现的生产方命令是 `node scripts/browser-runtime/build.mjs`：它在 `target/browser-runtime/package/` 中构建并执行 `npm pack`，将归档、摘要及凭据写到 `target/browser-runtime/`。产品从自身 `vendor/` 目录安装该确切归档并维护独立锁文件。[浏览器启动指南](./docs/browser-runtime.zh-CN.md)记录构建／安装／测试步骤。无需先发布到 Registry。开发链接仅为可选便利方式，不能作为验收证据。

**验收**

隔离的产品检出安装确切归档、进行类型检查、构建，并在无法访问引擎检出的情况下通过生产产物显示真实输出。缺失 WASM 或无效资源位置产生经过测试的诊断。验证公开 JS／类型／WASM 一致性和资源包含情况；仅列出归档文件或测试生产方开发服务器并不足够。

**不在范围内：** Registry 发布、额外公开包、前端 monorepo 基础设施、所有打包器适配，以及 SSR／Node.js GPU 支持。

## M5-04——Player MVP

**当前状态：** 参数、通道预览、最新请求处理及 PNG 导出均已实现，并通过上述有界本地验收。下述流程门槛已覆盖；M5-05 继续负责 1K 跨端质量及正式浏览器矩阵。

**范围**

- 打开用户 `.mix`；查询 Rust 所有的契约及现有公开绑定，显示当前有效值和合法控件；应用公开覆盖，不重写节点语义。
- 提供请求通道选择、2D 预览、结构化诊断，以及遵循文档化颜色／标量／法线编码的纹理下载。文件选择、图像编码、命名和下载属于产品。
- 保持一个活动渲染和一个可替换的最新待处理请求。拒绝过期／重复完成，最新请求失败后保留明确标为过期的预览，在产品退出时销毁实例。不再关心某个结果不代表 GPU 取消。
- 共用内部 runtime-client、预览、控件和文件模块；Studio 路由／编辑器推迟至 M5 验收之后。

**验收**

陶瓷、皮革和木材完成打开 → 修改公开参数 → 选择通道 → 预览 → 导出。检查无效源、无效覆盖、不支持的 GPU、最新请求失败和快速编辑。独立于屏幕预览检查导出尺寸／像素及颜色元数据，包括线性标量／法线数据。快速 UI 测试可以控制完成顺序，但实际渲染与导出门槛使用已安装运行时。

**不在范围内：** 图编辑、撤销重做、中间节点预览、3D 场景、在 `.mix` 中保存布局、账号、工程容器及可嵌入 Player 发布。

## M5-05——浏览器回归与 Alpha 就绪

**范围与验收门槛**

1. **跨运行时语义：** 使用相同源字节、节点版本、覆盖、分辨率和请求通道。在相同实现下比较规范化计划语义／哈希；源／构建身份与语义哈希分别保留。
2. **材质质量：** 对全部三种已接受材质，以 1024×1024、现有验收变体及请求的 baseColor／normal／roughness／height 通道执行。记录像素误差、平铺、非退化及参数因果性度量。测量浏览器差异，在接受运行之前评审并冻结明确的逐通道容差门槛；保留用于论证容差的失败。不放宽原生基准，也不承诺通用逐字节一致。
3. **生命周期与失败：** 验证后续渲染／销毁后保留像素仍自有可用、重复初始化／渲染／销毁、并发调用拒绝、最新结果行为、丢失／清理及有界保留状态。记录工作负载与内存核算限制；不得将描述符计数称为实测物理 GPU 内存。
4. **打包产品：** 重复隔离 tarball 安装、公开类型检查、生产构建及静态部署／浏览器执行。绑定两个仓库及消费归档；验证部署资源确实加载。Vite 开发服务器通过并不足够。
5. **自动化与来源：** 保留可复现浏览器测试及具体固定版本的 CI 环境。记录浏览器版本、OS、可获得的适配器／后端、所需 limits／features、flags、工具版本、源／锁／夹具／归档身份和结果。不支持、不可用或跳过的浏览器 GPU 执行是开放门槛，不算通过。保留现有四项原生必需检查；新增必需浏览器检查须与实时保护政策协调。
6. **就绪评估：** 依照现有保留政策保存成对浏览器验收／兼容性摘要及必要证据。列出已测试环境与剩余限制；Alpha 不代表广泛的稳定支持保证。

产品提供 `npm run test:browser`、`npm run test:materials` 和 `npm run test:deployment`；[比较指南](./docs/browser-materials.zh-CN.md)记录引擎命令。[M5-05 验收](./docs/evidence/m5-05/README.zh-CN.md)保留固定 CI 环境、先冻结后验收的门槛、全部矩阵像素与限制。该实测范围内门槛已通过；未测试环境仍无验收结论。

实际 npm Alpha 发布是在就绪之后进行的单独分发动作：确认包所有权，选择预发布版本和非 `latest` 标签，检查确切归档及发布说明，并通过产品精确版本依赖升级验证已发布软件包。运行时包、`.mix` 格式、节点语义和产品版本分别管理。本地 tarball 验收不得描述为 Registry 发布。

**不在范围内：** 仅为关闭工程门槛而发布、新的原生硬件保证、框架插件、零拷贝互操作、Studio MVP 及 M6 资源打包。

## 完成记录与后续范围

每项完成时，添加真实 PR／提交标识及绑定已测源和命令的精简结果记录。普通重复输出留在 CI 附件或忽略的本地目录；按政策保留已接受材质／视觉决策及必要支持内容。不覆盖 M3／M4／M4.1 历史结果，也不在计划中编造完成回执。

M5 通过后，再在产品仓库规划 Studio MVP：图编辑、连线、参数、撤销重做、公开绑定及标准 `.mix` 保存。布局可使用单独 `.mix.layout.json`，不得成为渲染依赖。其最终验收路径是 Studio 保存的 `.mix` → 独立 Player → 原生 CLI。这属于未来产品里程碑，不是引擎 M6：[M6](./ROADMAP.zh-CN.md#m6--资源与可移植打包)继续保留现有资源／便携资产进入条件。3D 预览和可嵌入 Player 包需要另有明确产品需求。
