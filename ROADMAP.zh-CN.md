# 路线图

[English](./ROADMAP.md) | 简体中文

**当前里程碑：** M4 与 [M4.1 仓库维护](./docs/evidence/m4-1/README.zh-CN.md)已验收。[M5 浏览器执行与打包消费](./docs/browser-runtime.zh-CN.md)进行中，2026-09-12；完整浏览器验收仍开放。

**实现状态：** PR-001 至 PR-015 已实现。`.mix` 图路径、十一个节点及三种已接受的 1K 材质具有本地 Metal 和固定 SwiftShader 证据；[M3 评审](./docs/m3-review.zh-CN.md)记录质量、release 测量及有界 2K 分配。[M4 计划](./M4_PRS.zh-CN.md)验证公开 Rust／CLI 契约、失败、过期结果、有界保留和实际包消费。版本 `8b43c84` 已完成[远端 CI 验收](./docs/evidence/remote-ci/README.zh-CN.md)，新增干净检出的 Linux／macOS／Windows CPU 检查及 Linux 固定 SwiftShader smoke、打包消费者、三种 1K 材质和 2K 跟踪。此前暂缓的 M0／M1 平台门槛已在此矩阵范围内关闭。兼容性及未测试硬件限制见[发布状态](./docs/release.zh-CN.md)。软件包仍未发布。轻量浏览器绑定和独立产品仓库现已存在；浏览器启动指南记录已实现的棋盘格切片和剩余 M5 门槛。

本路线图按可验证的结果组织，而不是按日期、季度、节点数量或功能数量安排。只有全部退出标准在干净环境中通过，里程碑才算完成。

## 路线图规则

1. 先完成当前里程碑，再提前引入后续范围。
2. 每个里程碑都必须保持主要命令路径通过。
3. 工程证据不能替代材质视觉质量或外部使用方证据。
4. 除非用于修复阻塞缺陷，里程碑退出标准不需要的功能均不在范围内。
5. 文末的停止规则优先于有吸引力但未经证实的后续工作。

---

## M0 — 清晰的基础工程

### 目标结果

建立小型 Rust 仓库，使人类或编码智能体能够克隆、理解、验证和扩展，不继承旧 Mixture 架构。

### 范围

- Rust 工作区包含：
  - `mixture-core`；
  - `mixture-wgpu`；
  - `mixture-cli`；
  - 私有 `xtask` 工具。
- `README.md`、`AGENTS.md`、`ARCHITECTURE.md`、`ROADMAP.md` 和 `INITIAL_PRS.md`。
- 固定的 Rust 工具链和 edition；
- 格式、Clippy、单元测试、文档和依赖策略命令；
- Linux、macOS 和 Windows 的非 GPU CI 检查；
- MIT/Apache-2.0 双许可证文件；
- 初始 ADR 目录和架构决策；
- 空的夹具和示例目录，并附有说明文档。

### 退出标准

以下命令在受支持开发平台的干净克隆中通过：

```bash
cargo xtask check
```

同时满足：

- 没有 crate 将占位运行时行为宣称为已实现；
- 文档中的仓库路径全部存在；
- CI 使用纳入版本控制的锁文件，并指定 `--locked`；
- 各文档中的公共使命、非目标和职责边界一致。

### 不在范围内

- 适配器获取；
- WGSL；
- `.mix` 解析；
- 渲染；
- 浏览器相关工作。

---

## M1 — 无界面 `wgpu` 纵向实现

### 目标结果

证明项目可以显式获取 GPU 上下文、执行一次离屏计算、回读像素，并解释失败原因。

### 范围

- 带有调用方可见选项的显式 `GpuContext`；
- 适配器、后端、设备限制和特性诊断；
- 稳定的 GPU 错误码和阶段；
- 面向人类和 JSON 两种模式的 `mixture doctor`；
- 一个内置的 64x64 棋盘格计算着色器；
- 离屏纹理分配；
- 纹理到缓冲区的回读；
- CLI 中的 PNG 编码；
- 使用固定版本软件适配器的独立 GPU 冒烟工作流；
- 一份真实硬件验证记录。

### 退出标准

以下命令：

```bash
mixture doctor --json
mixture render-builtin checker --size 64 --out checker.png
```

必须证明：

- 实际选择了适配器和后端；
- 计算调度和回读成功；
- 固定版本软件适配器上的棋盘格图像具有确定性；
- 没有可用适配器时，返回结构化且可操作的失败信息；
- 没有窗口表面、帧循环、图格式或隐藏回退。

### 不在范围内

- `.mix` 文档；
- 节点注册表；
- 资源池；
- WebAssembly。

---

## M2 — `.mix` v1 图 MVP

### 目标结果

能够解析、验证小型 `.mix` 图，将其编译为确定性的 `RenderPlan`，检查执行计划，并通过现有 `wgpu` 路径渲染。

### 范围

- `.mix` v1 JSON 模型；
- 严格解码与保守预算；
- 节点、强类型端口、参数、版本、边和暴露参数；
- 环检测与稳定拓扑排序；
- 按输出裁剪依赖；
- 强类型 `RenderPlan` 和稳定计划哈希；
- `validate`、`inspect --plan` 和支持图的 `render` 命令；
- 初始内置节点：
  1. `constant-scalar`；
  2. `constant-color`；
  3. `checker`；
  4. `levels`；
  5. `blend`；
  6. `material-output`；
- 每个节点的专项夹具和着色器检查；
- 请求通道的原始像素与 PNG 输出，包括连接／默认值来源状态。

### 退出标准

至少三个可读示例完成以下路径：

```text
.mix -> validation -> RenderPlan -> wgpu -> requested PNG outputs
```

并满足：

- 计划哈希具有确定性；
- 格式错误文档的诊断稳定；
- 有测试证明按请求输出裁剪无关分支；
- 仅核心测试不初始化 GPU；
- CLI 不重复实现图或节点语义；
- 不引入通用优化器或二进制格式。

### 不在范围内

- 真实感材质质量声明；
- 图像资源；
- 子图；
- 预设；
- 浏览器绑定。

---

## M3 — 材质 MVP 与质量验收

### 目标结果

证明小型节点集能在 1K 分辨率下生成三种有用且已验收的材质，并处理至少一个有代表性的 2K 工作负载，且中间资源不会无限增长。

### 范围

仅添加基准材质需要的节点，总数不超过十二个：

- `fractal-noise`；
- `gradient-map`；
- `transform-2d`；
- `warp`；
- `height-to-normal`；
- 一个预留节点，仅在有记录的材质阻塞问题证明必要时添加。

建立三种基准材质：

1. **釉面陶瓷／棋盘格**：图基础、颜色、粗糙度、文档规定的中性法线默认值、高度和无缝平铺；后续可以采用生成法线，但不改变原有验收意图。
2. **类皮革表面**：微表面高度、参数因果关系、粗糙度和法线响应。
3. **各向异性木纹表面**：方向性结构、变换、扭曲和缩放行为。

添加：

- 受保护的基准检查和基准更新流程；
- 软件适配器预期输出；
- 具有容差的真实适配器比较；
- 平铺接缝指标；
- 非有限值与输出范围检查；
- 用于验证参数因果关系的变体；
- 人工视觉审查记录；
- 三种材质全部通过 1K 验收；
- 最复杂材质的 2K 分配跟踪；
- 仅在测量证明有需要时，在最后一个使用者完成后释放资源并复用兼容纹理；
- 在 `inspect` 和渲染报告中提供峰值字节数与计算通道数。

### 退出标准

三种材质都必须：

- 验证和编译通过，不得用警告掩盖缺失行为；
- 渲染请求的 baseColor、normal、roughness 和 height 通道；
- 通过固定版本软件适配器基准；
- 通过基于容差的真实适配器检查；
- 通过平铺与非退化性验收；
- 文档规定的参数变体能产生有意义的输出变化；
- 具有已接受的人工审查记录；
- 通过唯一的 `wgpu` 路径在 1024x1024 下渲染。

至少一种材质必须在文档规定的临时内存预算内完成 2048x2048 渲染，或者提供证据说明所需的最小生命周期优化。

全部 M3 验收项通过前，不得添加第十三个内置节点。

### 不在范围内

- 节点编辑器；
- 内嵌图像；
- 自定义着色器；
- 引擎配置；
- 公共市场内容。

---

## M4 — 稳定的原生 SDK

### 目标结果

独立的原生使用方可以使用 Mixture，无需导入仓库私有实现，也不依赖相对源码路径。

### 范围

- 经过审查的解析、验证、编译、渲染和诊断公共 Rust API；
- 经过审查的稳定 CLI JSON schema 和退出码；
- 适合长时间渲染的显式取消或过期结果处理；
- 设备丢失和内存不足诊断；
- 有界的管线／资源缓存；
- 仅使用公共 crate 或已构建 CLI 的软件包使用方测试；
- 一个独立的 Node.js 或 Rust 使用示例；
- 发布检查清单与兼容性策略。

Node.js 集成应从启动 CLI 子进程开始。只有性能分析证明进程启动构成实际瓶颈，才考虑 N-API、守护进程或自定义 IPC。

### 退出标准

独立使用方夹具可以完成以下路径：

```text
load .mix
-> validate
-> override exposed parameters
-> request channels
-> render
-> consume outputs and structured metrics
```

且无需导入工作区内部实现。

公共文档、示例、CLI JSON 和库行为必须一致。

### 不在范围内

- WebAssembly；
- 编辑器 UI；
- 二进制打包；
- 核心中的特定引擎导出器。

---

## M4.1 — 仓库维护

**已验收实现：** `cc98dc9298add5ed172e0c1752d8a768b61a0eb5`，通过 [PR #1](https://github.com/OpenMixture/OpenMixture/pull/1) 合入。[完成记录](./docs/evidence/m4-1/README.zh-CN.md)绑定已通过的初始主分支／PR／集成主分支检查、生效保护、默认分支及保留证据。后续文档修改需要独立的 PR 与主分支检查。

### 目标

已接受的 M4 实现可从受保护的默认 `main` 查阅，后续变更具有真实 PR 记录及可保留的验收证据。此维护范围于 2026-09-12 确定，不重开 M4 验收，也不授权 M5 运行时工作。

### 范围

- 从已接受历史建立 `main`，不强推或删除历史分支；验证其 CI 并设为默认分支。
- 应用[可审查的分支规则](./.github/main-ruleset.json)：要求 PR、解决对话、禁止强推／删除、四项必需 GitHub Actions 检查，以及适用于单维护者流程的零批准数量。
- CI 响应 PR、推送到 `main` 及手动触发，保留 GPU 串行执行、驱动固定版本／缓存验证及每项验收。
- 区分历史实施标识 `PR-001` 至 `PR-015` 与真实 GitHub PR 编号。
- 同步当前中英文状态，采用[证据保留规则](./docs/evidence-policy.zh-CN.md)，描述已验证环境而不虚构支持保证。

### 退出标准

- 实时 GitHub 状态确认正确默认分支及生效保护，当前集成版本的四项必需检查全部通过。
- 真实 PR 经保护路径合入工作流／文档变更，记录其当前检查、合并版本及后续主分支运行。
- `cargo xtask check` 通过，当前文档与证据一致；历史来源包、基准、人工回执及绑定视觉证据不变。
- 精简完成回执标识分支、版本、运行、已应用规则及任何描述性源码检查点标签，不声称软件包发布。

[治理指南](./docs/governance.zh-CN.md)定义启用顺序及验证。运行时／API 修改、新节点、M5 实现、M6、大型工具重构及软件包分发均不在范围内。

---

## M5 — WebAssembly 与浏览器 WebGPU

**规划进入，2026-09-12：** M4／M4.1 验收之后，[M5-01](./M5_PRS.zh-CN.md)记录选定的交付方向。该规划项编写成对计划与[浏览器 SDK 契约](./docs/browser-sdk.zh-CN.md)，不实施绑定、不创建产品仓库、不发布软件包，也不声称浏览器验收。M5-02／M5-03 实施现已启动，见[浏览器启动指南](./docs/browser-runtime.zh-CN.md)。M5-04／M5-05 验收仍开放。

### 目标结果

独立 Player 安装一个完整浏览器运行时包，使用与原生消费者相同的 `.mix`、Rust 节点契约、编译器、RenderPlan 语义和 WGSL。

### 范围

- 引擎与浏览器包构建保留在本仓库；增加 `mixture-wasm`，作为 `mixture-core` 与唯一 `mixture-wgpu` 执行器的轻量绑定。
- 适配浏览器目标 features、异步执行／回读与失败交付，同时保持原生行为。
- 交付单一软件包，暂名 `@openmixture/runtime`，包含同一构建产生的 JS 接口、TypeScript 声明和 WASM；消费者不需要 Rust 工具链。
- 提供显式 WASM／GPU 初始化、无需 GPU 的验证与节点契约查询、现有公开参数覆盖、请求通道、自有 RGBA8 和结构化诊断。
- 使用一个独立产品仓库，暂名 `OpenMixture/Studio`，先 Player、后 Studio。它通过公开包负责文件、控件、2D 预览、最新请求处理及导出。
- 验证真实 tarball 安装和一种固定版本的 Vite 消费方式，包括生产静态部署和可配置 WASM 资源路径。
- 增加三种材质的浏览器／原生回归、生命周期／失败证据及 Alpha 就绪评估。Registry 发布为单独分发动作。

[实施计划](./M5_PRS.zh-CN.md)按 M5-01 契约 → M5-02 浏览器执行 → M5-03 打包消费者 → M5-04 Player MVP → M5-05 验收排序。拟议命名不代表已创建仓库或拥有包名。独立 Player 承担外部 Web 消费者验收，无需另建完整演示。

### 退出标准

隔离的产品消费者可以完成：

```text
install the runtime tarball
-> load .mix bytes
-> validate and apply exposed parameter overrides
-> request outputs
-> render through browser WebGPU
-> display and export textures
```

它必须使用软件包公开入口，不依赖生产方私有导入、源码相对资源、Rust 安装或编译型 postinstall。生产构建资源必须在静态服务下运行，包括非根 base path。JS、声明与 WASM 必须对应记录的归档／构建。

Rust 仍为语义权威。原生与浏览器计划在相同请求下必须语义等价。三种基准材质及其现有验收变体均以 1K 运行，采用经过评审的逐通道像素容差、平铺、非退化及参数因果性检查；原生基准继续受保护。返回像素在后续渲染和运行时销毁后仍可用。无效输入、不支持的环境、并发调用、设备丢失、清理及过期结果均具有经过测试的结果。

验收记录标识两个仓库、归档／锁／夹具身份、确切浏览器／OS／工具链及可观察的适配器／后端、所需 limits／features／flags、比较标准、命令及门槛结果。浏览器环境和数值容差在实施中、验收前固定；跳过 GPU 执行不算通过。现有原生必需检查保持完整，浏览器构建／回归须能在文档化 CI 环境中复现。不得只凭规划文档或原生 GPU 证据关闭 M5。

### 不在范围内

- 其他像素执行器、WebGL2 或 CPU 回退；
- 单独编写的 TypeScript 节点注册表／编译器；
- 节点创作 UI、中间节点预览及 3D 预览；
- SSR、Node.js GPU、所有打包器适配及 GPU 纹理互操作；
- 引擎与共用产品仓库之外的独立 SDK／Player 仓库；
- M6 资源／容器格式、新节点及未经发布决定的软件包发布。

M5 验收后，可将 Studio MVP 规划为产品里程碑，输出供 Player 和原生 CLI 使用的标准 `.mix`。它不更名或启动引擎 M6。

---

## M6 — 资源与可移植打包

### 进入条件

只有真实使用方需要图像资源或便携的单文件资产，并且已证明普通 `.mix` 加外部文件不足以满足需求时，才开始 M6。

### 目标结果

能够引用、验证和分发有界的外部图像资源，同时不削弱核心信任边界。

### 可能范围

- 图像输入节点；
- 按内容寻址的资源与哈希；
- 安全的相对路径；
- 资源预算和解码限制；
- 可选的 `.mixpack` 容器；
- 增量资源缓存；
- 与 `mixture-core` 隔离的旧版转换工具。

### 退出标准

至少一个真实使用方采用资源工作流，所有资源字节、标识、限制和失败模式都可检查、可测试。

### 不在范围内

- 专有项目导入；
- 任意可执行着色器；
- 将编辑器状态嵌入运行时文档。

---

## 后续候选方向

以下内容尚未安排为里程碑：

- 节点编辑器；
- 子图和可复用函数；
- 预设；
- 自定义 WGSL 或插件；
- 特化纹理格式；
- 计算通道融合或通用编译器优化；
- CPU 渲染器；
- WebGL2 渲染器；
- Unity、Unreal、Godot 或 glTF 导出配置；
- 市场、账号、协作或云渲染；
- 3D 烘焙；
- AI 辅助材质生成。

每个候选方向都需要基于真实使用方或实测阻塞问题，做出新的路线图决策。

## 停止规则

1. 全部 M3 基准材质验收通过前，不添加第十三个节点。
2. 原生 CLI 和 `.mix` 契约尚未稳定到能够供 M4 使用方使用时，不开展 WebAssembly 工作。
3. Web 使用方成功使用轻量 M5 API 之前，不开发节点编辑器。
4. 未测得真实打包或加载问题之前，不采用二进制格式。
5. 不增加第二个像素后端。
6. 不进行隐式语义回退。
7. 计算通道数、内存或运行时间数据尚未证明必要性前，不引入通用优化器。
8. 没有实际边界需求，不新增 crate。
9. 没有可审查的前后对照证据，不更新基准。
10. 任何功能都不能仅因生成非空像素而宣称成功。
11. 特定引擎关注点不得进入 `mixture-core`，除非它属于通用材质语义。
12. 文档中的主要命令路径或必需 CI 未通过时，不关闭里程碑。
